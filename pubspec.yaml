name: my_flutter_app
description: A Flutter application integrated with Keycloak.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  dio: ^5.0.0
  flutter_secure_storage: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true